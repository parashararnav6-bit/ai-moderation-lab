name: ai-moderation-lab
description: Multi-agent AI moderation environment with reward-based evaluation

entry_point: app:app

tasks:
  - name: easy
    description: Basic moderation cases
  - name: medium
    description: Context-aware moderation
  - name: hard
    description: Adversarial moderation scenarios

observation_space:
  type: object

action_space:
  type: object

reward_range:
  min: 0.0
  max: 1.0